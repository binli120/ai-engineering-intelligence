# LangGraph example

This directory is reserved for a tested, version-pinned example covering:

- typed state,
- deterministic nodes,
- conditional routing,
- checkpoint persistence,
- interruption for approval,
- resumption,
- failure and idempotency tests.

The example is intentionally deferred until the model provider and persistence backend are selected.
