# OpenAI Agents SDK example

This directory is reserved for a tested, version-pinned example covering:

- one agent,
- a typed function tool,
- structured output,
- tracing,
- usage accounting,
- an approval boundary,
- deterministic tests.

The example is intentionally deferred until an API key and target model policy are selected.
